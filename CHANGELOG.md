# 更新日志

一个版本一节，标题写「版本 · 日期 · 渠道」（渠道是 alpha / beta / rc / stable），正文一行一条、面向用户的措辞。
发版时 CI 从这里取该版本的说明写进 GitHub Release，并汇总成官网下载页用的 `releases.json`（`tools/release/releases_json.py`）。

## 0.1.7 · 2026-09-17 · beta

- 英文译词带上美式音标，按课本 / 词典写法显示：`开发` → v. develop (dɪˈveləp)；人名、地名等没有音标的只显示译词
- 「设置 → 通用」新增「显示译文音标」开关（缺省开）：关掉后英语音标与日语假名都不再显示；选「不显示译文」时该项不可用
- 修复 Notion、语雀这类网页编辑器里输入拼音时弹出加粗 / 链接浮动格式条的问题：拼音串不再处于选中状态
- 「设置 → 通用」的从属选项（如「但在终端和代码编辑器里不给」）缩进到父项之下，层级更清楚

## 0.1.0-alpha.6 · 2026-09-17 · alpha

- 英文译词带上美式音标，按课本 / 词典写法显示：`开发` → v. develop (dɪˈveləp)；人名、地名等没有音标的只显示译词
- 「设置 → 通用」新增「显示译文音标」开关（缺省开）：关掉后英语音标与日语假名都不再显示；选「不显示译文」时该项不可用

## 0.1.6 · 2026-09-17 · beta

- 新增领域词库「AI 与软件开发」（缺省关，「设置 → 词库」勾选）：AI 术语、各家模型名、Git、框架、包管理器与配置文件名；英文模式敲前缀补全完整名称，如 claudec → Claude Code、packagej → package.json
- 基础词库补进 1357 个常用词：取餐、弹出、弹窗、播放器、这时、长按、点赞、购物车等（上游 #121、#98、#127）
- 修复拼音切分偏向后鼻音：henganrao → 很干扰、dangao → 蛋糕、dangaodian → 蛋糕店，不再出「能看到」「当奥」；按音节移动光标与删除也跟拼音行显示一致（上游 #120、#98）
- 新增「智能ABC」双拼方案（上游 #112）
- 「设置 → 通用」可关闭 emoji 候选，也可关闭中文模式里的英文单词候选（上游 #104、#79）
- 「设置 → 候选窗口」可调文字大小（12–28，缺省 16），译词与序号按比例跟着变；只对微明渲染器生效（上游 #34）

## 0.1.0-alpha.5 · 2026-09-17 · alpha

- 新增领域词库「AI 与软件开发」（缺省关，「设置 → 词库」勾选）：AI 术语、各家模型名、Git、框架、包管理器与配置文件名；英文模式敲前缀补全完整名称，如 claudec → Claude Code、packagej → package.json
- 基础词库补进 1357 个常用词：取餐、弹出、弹窗、播放器、这时、长按、点赞、购物车等（上游 #121、#98、#127）
- 修复拼音切分偏向后鼻音：henganrao → 很干扰、dangao → 蛋糕、dangaodian → 蛋糕店，不再出「能看到」「当奥」；按音节移动光标与删除也跟拼音行显示一致（上游 #120、#98）
- 新增「智能ABC」双拼方案（上游 #112）
- 「设置 → 通用」可关闭 emoji 候选，也可关闭中文模式里的英文单词候选（上游 #104、#79）
- 「设置 → 候选窗口」可调文字大小（12–28，缺省 16），译词与序号按比例跟着变；只对微明渲染器生效（上游 #34）

## 0.1.5 · 2026-09-17 · beta

- 新增「繁体输出」：「设置 → 通用」打开后候选与上屏文字转成繁体（只换字形），学习、译词、云端词照常（上游 #41）
- 中英混杂词直接按「字母 + 拼音」打：cpan → C盘、bzhan → B站、youpan → U盘（与 优盘 并列）、txu → T恤、agu → A股（上游 #93）
- 翻页键可选「- =」：「设置 → 快捷键」翻页键多一项，缺省仍是 [ ]；选它时输入拼音中敲 - 是翻页，不再进英文直输（上游 #43、#116）
- 学习语言可选「不显示译文」（配置 `learning_language = "off"`）：候选旁不出译词，生词标记与释义兜底一并停；Windows 换学习语言不再需要重启（上游 #123）
- macOS：系统设置「键盘 → 文本替换」里的条目在输入拼音时也可用，输入码敲全后短语占该码最靠前的空位；「自定义短语」页可关（上游 #84）
- 中英混输可选「中文优先」（缺省关）：开着时整段输入是英文词（hello、key）也让中文候选排第一、英文词固定第二，空格上屏的是中文；关着时拼音不成立的输入英文词排第一。「设置 → 通用」勾选（上游 #85）
- `?` 不再自动进入问字：没在组句时敲 ? 就是问号；想保留旧行为到「设置 → 快捷键」勾选（上游 #83）
- 「lüe / nüe」类音节 lue / nue 与 lve / nve 两种写法都能稳定命中（如 celue → 策略），双拼方案尤其受益（上游 #59、#60）
- 私密输入（Windows 的密码框与无痕窗口、macOS 的 Secure Input）里的词不再记进词汇记录；多个应用同时在输入时各自的组句与学习互不干扰（上游 #89）
- 候选窗口（Windows 还有悬浮状态条）改由微明自己的渲染器绘制，两平台像素一致，为主题铺路：「设置 → 候选窗口」可选「渲染引擎」与「字体」（系统里装的任一字体）；不合适可切回「系统绘制」
- 「高级」页新增「学习输入习惯」开关：关掉后不再按你的选择调整候选顺序、不再记新词与敲错纠正；已学过的照常生效，随时可再打开
- 本版合入上游青简的改动，含其社区贡献：@luojiyin1987（ü 韵母写法）、@broxlighters（会话隔离与私密记录）、@9tong（问字开关、打包脚本）

## 0.1.0-alpha.4 · 2026-09-17 · alpha

- 新增「繁体输出」：「设置 → 通用」打开后候选与上屏文字转成繁体（只换字形），学习、译词、云端词照常（上游 #41）
- 注音模式下数字键与 - ; , . / 都进组句，选词改用 Enter，Shift+Enter 原样上屏，空格是一声（上游 #41）
- 修复导入 Rime .dict.yaml 词库后候选里没有词的问题：导入时转成微明格式并自动启用，可一次多选；词库目录里新增、替换、移除的文件一秒内生效，不用重启输入法（上游 #36、#118）
- 中英混杂词直接按「字母 + 拼音」打：cpan → C盘、bzhan → B站、youpan → U盘（与 优盘 并列）、txu → T恤、agu → A股（上游 #93）
- 翻页键可选「- =」：「设置 → 快捷键」翻页键多一项，缺省仍是 [ ]；选它时输入拼音中敲 - 是翻页，不再进英文直输（上游 #43、#116）
- 数字与标点按当前系统键盘布局识别：非美式布局（如德语）下以前会按美式键位出错误的符号（上游 #115）
- 学习语言可选「不显示译文」（配置 `learning_language = "off"`）：候选旁不出译词，生词标记与释义兜底一并停；Windows 换学习语言不再需要重启（上游 #123）
- 中英混输可选「中文优先」（缺省关）：开着时整段输入是英文词（hello、key）也让中文候选排第一、英文词固定第二，空格上屏的是中文；关着时拼音不成立的输入英文词排第一。「设置 → 通用」勾选（上游 #85）
- `?` 不再自动进入问字：没在组句时敲 ? 就是问号；想保留旧行为到「设置 → 快捷键」勾选（上游 #83）
- 「lüe / nüe」类音节 lue / nue 与 lve / nve 两种写法都能稳定命中（如 celue → 策略），双拼方案尤其受益（上游 #59、#60）
- 私密输入（Windows 的密码框与无痕窗口、macOS 的 Secure Input）里的词不再记进词汇记录；多个应用同时在输入时各自的组句与学习互不干扰（上游 #89）
- 候选窗口（Windows 还有悬浮状态条）改由微明自己的渲染器绘制，两平台像素一致，为主题铺路：「设置 → 候选窗口」可选「渲染引擎」与「字体」（系统里装的任一字体）；不合适可切回「系统绘制」
- 「高级」页新增「学习输入习惯」开关：关掉后不再按你的选择调整候选顺序、不再记新词与敲错纠正；已学过的照常生效，随时可再打开
- 日志集中到一个目录「%LOCALAPPDATA%\Glimmer\logs」：引擎、各应用内的输入法部分、设置程序各一个前缀，按天分文件、保留 7 天；设置程序「关于」与「高级」页新增「打包日志到桌面」，反馈问题发一个 zip 即可
- 设置程序启动失败时会弹出提示并写日志，不再静默退出
- 安装包多带一份 32 位输入法组件：企业微信、WPS、32 位 QQ 这类 32 位应用里之前只能出字母、或被系统换回别的输入法，现在能正常打字
- 输入法组件不再依赖 VC 运行库（vcruntime140.dll）：没装运行库、或运行库文件被别的安装程序改坏了权限的机器上，任务栏搜索、开始菜单这类系统应用会因为加载不了微明而悄悄切回上一个输入法，现在不会了
- 修复企业微信、微信、Notepad++ 这类自绘输入框里数字键和英文标点没反应的问题：没在组句时敲的数字、英文模式下敲的标点之前会被吞掉（上游 #25、#26）
- 附加词库只从用户目录的 dicts\ 装，不再把学习数据文件当词库加载
- **已知问题：本版没有代码签名，安装时 SmartScreen 会拦截一次；在任务栏搜索、「设置」等系统应用中候选窗口可能被遮挡。腾讯软件管理这类带反注入保护的安全软件内无法输入，是其自身机制，除微软拼音外的输入法均如此**
- 本版合入上游青简的改动，含其社区贡献：@luojiyin1987（ü 韵母写法）、@broxlighters（会话隔离与私密记录）、@9tong（问字开关、打包脚本）

## 0.1.0-linux.3 · 未发布 · alpha

- 新增「繁体输出」：「设置 → 通用」打开后候选与上屏文字转成繁体（只换字形），学习、译词、云端词照常（上游 #41）
- 中英混杂词直接按「字母 + 拼音」打：cpan → C盘、bzhan → B站、youpan → U盘（与 优盘 并列）、txu → T恤、agu → A股（上游 #93）
- 学习语言可选「不显示译文」（配置 `learning_language = "off"`）：候选旁不出译词，生词标记与释义兜底一并停；Windows 换学习语言不再需要重启（上游 #123）
- 中英混输可选「中文优先」（缺省关）：开着时整段输入是英文词（hello、key）也让中文候选排第一、英文词固定第二，空格上屏的是中文；关着时拼音不成立的输入英文词排第一。「设置 → 通用」勾选（上游 #85）
- `?` 不再自动进入问字：没在组句时敲 ? 就是问号；想保留旧行为到「设置 → 快捷键」勾选（上游 #83）
- 「lüe / nüe」类音节 lue / nue 与 lve / nve 两种写法都能稳定命中（如 celue → 策略），双拼方案尤其受益（上游 #59、#60）
- 私密输入（Windows 的密码框与无痕窗口、macOS 的 Secure Input）里的词不再记进词汇记录；多个应用同时在输入时各自的组句与学习互不干扰（上游 #89）
- 「高级」页新增「学习输入习惯」开关：关掉后不再按你的选择调整候选顺序、不再记新词与敲错纠正；已学过的照常生效，随时可再打开
- 本版合入上游青简的改动，含其社区贡献：@luojiyin1987（ü 韵母写法）、@broxlighters（会话隔离与私密记录）、@9tong（问字开关、打包脚本）

## 0.1.4 · 2026-09-17 · beta

- 五笔新增 98 五笔与新世纪五笔：「偏好设置 → 通用」的「五笔」菜单里选，按键与 86 五笔完全一致
- 五笔同一编码的多个候选改成按码表原有顺序排列

## 0.1.0-linux.2 · 2026-09-17 · alpha

- 支持 Fcitx5（KDE Plasma 等默认用 Fcitx5 的桌面）：同一个安装包，装好后运行 `fcitx5 -r`，在「Fcitx5 配置」里把「微明」加进输入法列表；需要 Fcitx5 5.1 或更新（Ubuntu 24.04、Debian 13 或更新）
- 修复上一版只能装在 Ubuntu 24.04 上：现在 Ubuntu 22.04、Debian 12 及更新的系统都能安装（IBus）
- Fcitx5 下中 / 英状态显示在托盘的输入法图标上，也可以点击切换
- **已知问题：「翻译选中文字」暂不可用；候选框由系统输入法面板显示，没有词性与生词标记**

## 0.1.0-linux.1 · 2026-09-16 · alpha

- Linux 版首个内测版：deb 安装包（x86_64 与 ARM64），适用于使用 IBus 的桌面（Ubuntu、Debian、Fedora 的 GNOME 等）；装好后在「设置 → 键盘 → 输入源」里添加「微明」
- 功能与 Windows 版一致：整句输入、简拼、拼写纠错、模糊音、双拼、五笔 86、大千注音、英文模式补全、emoji、本地整句模型、可选的云联想
- 单击 Shift 切换中 / 英，系统顶栏的输入法指示器上也可以点击切换
- 候选旁显示译词，数字键选词，`[` `]` 与 PageUp / PageDown 翻页
- 设置暂时只能改配置文件 `~/.config/glimmer/config.toml`，保存后即时生效
- **已知问题：暂不支持 Fcitx5；「翻译选中文字」暂不可用；候选框由系统输入法面板显示，没有词性与生词标记**

## 0.1.0-alpha.3 · 2026-09-16 · alpha

- 改名为「微明 Glimmer」，换了新图标；装好后在输入法列表里选「微明」
- 新增五笔 86 输入：「设置 → 通用」里打开，四码唯一自动上屏、顶字上屏、z 键临时用拼音反查、自动造词，状态条显示当前方案
- 新增大千注音输入：「设置」里打开，状态条显示「注」
- 打拼音时敲标点可以设置成先上屏候选再出标点，或按情况自动判断
- 学习语言新增西班牙语
- 修复 Windows 10 上设置程序打不开；系统要求放宽到 Windows 10 1809 及以上
- 第一次启动就写出带说明的配置文件，不用先在设置程序里保存一次
- **已知问题：内测版没有代码签名，安装时 SmartScreen 会拦截一次；在任务栏搜索、「设置」等系统应用中候选窗口可能被遮挡**

## 0.1.3 · 2026-09-16 · beta

- 改名为「微明 Glimmer」，换了新图标，输入法菜单里的图标改成跟随系统深浅色的矢量图；从青简升级时词频、用户词和设置自动搬过来
- 新增五笔 86 输入：「偏好设置 → 通用」里打开，四码唯一自动上屏、顶字上屏、z 键临时用拼音反查、自动造词
- 单击 Shift 切换中 / 英，也可以换成自定义组合键；修复微信、企业微信里切换状态不对的问题
- 修复 Chrome、Edge 等浏览器里单击 Shift 切不了中英、换输入框后偶尔敲不出字
- 新增「自定义短语」：给一段输入码固定放几个候选，在偏好设置里直接编辑
- 打拼音时敲标点可以设置成先上屏候选再出标点，或按情况自动判断；可设置默认用全角还是半角标点
- 学习语言新增西班牙语
- 偏好设置改成左侧栏加分组卡片的样式
- 修复半角标点模式下单独输入问号再按空格 / 回车时问号丢失
- 修复输入法菜单偶发崩溃，以及切换应用后菜单栏状态项变成空白块

## 0.1.2 · 2026-09-12 · beta

- 随包带一个本地整句模型（约 56 MB，全程离线）：敲完一段拼音停一下，整句候选换成模型认为更通顺的那句；会看光标前的几十个字，同一段拼音在不同上下文里给的句子可能不同；密码框里不读上下文
- 「偏好设置 → 云服务」新增「本地整句模型」开关，关掉只用词库统计，与云联想互不影响
- 基础词库新增 5398 条常整块打出的组合（我的 / 好的 / 不知道）与 48 条常用领域词（对齐 / 后端 / 词库…），整句转换的语言模型一并更新
- 输入日志升级到 v1，多记退格重打、直通字符、云端联想、上文断开等事件，仍只写本机、随时可关可清

## 0.1.0-alpha.2 · 2026-09-12 · alpha

- 随包带一个本地整句模型（约 56 MB，全程离线，CPU 推理几十毫秒）：敲完一段拼音停一下，整句候选换成模型认为更通顺的那句；会读应用里光标前的几十个字当上下文（记事本 / 浏览器 / 终端都支持），「设置 → 云服务」可关闭
- 密码框按 Windows 输入法规范处理：应用禁用了文本服务的密码框里所有按键直接放行、不组句（与 macOS 的 Secure Input 一致）；应用声明为私密的输入框里不读上下文、不学习、不记输入日志、不发云端
- 基础词库新增 5398 条常整块打出的组合与 48 条常用领域词，整句转换的语言模型一并更新
- 设置程序里的改动即时生效这条更可靠了：之前输入法在多个应用里同时开着时设置可能要等重启才生效
- 输入法日志按天分文件、只保留 7 天
- **已知问题：内测版没有代码签名，安装时 SmartScreen 会拦截一次；在任务栏搜索、「设置」等系统应用中候选窗口可能被遮挡**

## 0.1.0-alpha.1 · 2026-09-12 · alpha

- Windows 版首个内测版，功能与 macOS 版一致；安装包一键安装，装完即可在输入法列表中选择「青简」，升级时直接安装新版
- 单击 Shift 切换中 / 英，任务栏与桌面上可拖动的悬浮状态条都显示当前模式；状态条上可切换全角 / 半角标点，中英各记一份
- 候选窗口跟随光标，支持深色模式与横排 / 竖排，拼音行带内联下划线
- 整句输入、简拼、拼写纠错、模糊音、双拼、英文模式补全、中英混输、emoji、日期时间与算式、按码点输入字符
- 候选旁的译词与生词标记：Ctrl + 数字直接输出译词，Shift + 数字删除候选
- 可选的云联想：云端候选、整句补全、问字、Ctrl + Alt + T 翻译选中的文字
- 设置程序（开始菜单「青简设置」）与 macOS 偏好设置页面一致，修改即时生效
- **已知问题：内测版没有代码签名，安装时 SmartScreen 会拦截一次；在任务栏搜索、「设置」等系统应用中候选窗口可能被遮挡**

## 0.1.1 · 2026-09-08 · beta

- 中英混输：一句拼音的末尾接英文词直接成句，`woxiangxuehaorust` → 我想学好rust，`wodeid` → 我的ID；像 `database` 这种也能读成拼音的按哪种更通顺定
- 组句编辑：⌥⌫ 删一个音节、⌘⌫ 删到开头、⌥← / ⌥→ 按音节移动光标
- 组句中敲半角标点进入英文直输段，中文模式下也能直接打 `hello, world?`
- 学习更稳：退格撤销能回看最近四次上屏；一段拼音分几次选完的，合起来两次就成词；云端词按敲的读音学，不再学进错读音
- `key` 这类末尾落单一个字母的输入，选惯了中文词就中文排前面
- 菜单栏「中 / 英」状态项不再跳回最左边

## 0.1.0 · 2026-09-07 · beta

- 整句输入：一口气敲完一句再上屏，简拼也行；敲错一两个字母会按最像的读法给出候选
- 候选旁有词性和译词，英语 / 日语可选，日语带假名；⌥ + 数字直接打出译词；没见过几次的译词画橙色
- 模糊音、双拼（小鹤 / 自然码 / 微软 / 搜狗）、英文模式下的补全与拼错纠正、中英混输、emoji、日期时间与算式快捷输入
- 越用越顺：记住你选过的词和常连着打的词，自动造词，选错了退格重选会把学到的撤回
- 可选的云联想：云端候选词、整句补全、问字、翻译选中文字、缺少的译词自动补；缺省关闭，偏好设置里可测试连接
- 基础词库 8.7 万条，另有 11 本领域词库随包；可导入自己的词库（青简 TSV、Rime、.qj）
- 偏好设置分九页，含输入统计（今天 / 最近 7 天 / 累计打了多少字，学习语言的词汇量）
- 输入法崩溃也不丢学习数据、不丢正在打的字；安装包装完自动注册输入法
