---
title: 偏好设置
order: 1
description: 偏好设置各页的内容、打开方式、输入法菜单的内容，以及直接修改配置文件时的生效方式。
---

- **macOS**：从输入法菜单打开（菜单栏「中 / 英」状态项，或系统输入源菜单中微明的几项），名为「偏好设置」。
- **Windows**：开始菜单的「微明设置」，或悬浮状态条上的 ⚙，名为「设置」。

常规设置修改即时生效；自定义短语的启用开关即时生效，新增或编辑在点击「保存规则」后生效。

| 页 | 内容 |
|---|---|
| 通用 | 学习语言（英语 / 日语 / 西班牙语 / 不显示译文）、「显示译文音标」（英语音标与日语假名，不显示译文时不可用）、每页候选数 1 至 9、拼音方案（全拼 / 六套双拼 / 大千注音 / 关（只用五笔），见 [模糊音与双拼](../input/fuzzy-and-shuangpin.md#输入方案的两条轴)）、繁体输出开关、五笔（关 / 86 五笔 / 98 五笔 / 新世纪五笔；与拼音方案同时开着就是混输，只想用五笔就把拼音方案选「关」）及其「四码自动上屏」（混输下不生效，此项置灰）「显示编码提示」开关（见 [五笔输入](../input/wubi.md)）、英文模式候选开关与「终端 / 编辑器中不给候选」、「输入拼音时也给英文词候选」及其「中文候选排在英文词前面」「给 emoji 候选」；macOS 可选择默认中文标点全角或半角，字母和数字不变；Windows 另有中英文标点开关；「打拼音时敲标点」三选一：进入英文直输（缺省）/ 先上屏候选再出标点 / 自动 |
| 候选窗口 | 外观（跟随系统 / 浅色 / 深色）、排布（竖排 / 横排）、渲染引擎（微明渲染器 / 系统绘制）、字体（macOS 用搜索框筛选系统字族，列表里每个名字用它自己的字体显示；Windows 直接输入字族名，边输边提示；没装的字体自动回到系统字体，只对微明渲染器生效）、文字大小（候选词字号 12 至 28，缺省 16，译词与序号按比例跟着变，只对微明渲染器生效；Linux 的候选窗口由 Fcitx5 / IBus 绘制，字号跟随它们的主题）、拼音显示位置（行内 + 窗口 / 只在行内 / 只在窗口）；Windows 另有「悬浮状态条」开关 |
| 快捷键 | 翻页键对、算式与问字的前缀键、「没在输入拼音时敲 ? 也进入问字」、上屏第一 / 第二条译词的修饰键、删除候选的修饰键、翻译选中文字的组合键；「恢复默认快捷键」；页底列出组句中不可更改的固定键（全表见 [按键与快捷键](../getting-started/keys.md)） |
| 自定义短语（macOS） | 设置输入码、固定候选位置、任意长度文本与启用状态；支持新增、编辑、删除；「也使用系统设置里的文本替换」开关 |
| 模糊音 | 九条勾选 |
| 词库 | 随包领域词库开关，导入 / 开关 / 移除用户词库，见 [词库](dictionaries.md) |
| 云服务 | 本地整句模型开关（见 [本地整句模型](../input/local-model.md)）；云联想开关、云端词格数、服务地址、模型、API 密钥、「测试连接」按钮 |
| 统计 | 输入量与词汇量，见 [统计](../learning/statistics.md) |
| 高级 | 详细日志、打包日志到桌面、学习输入习惯开关、记录输入日志、清空输入日志、在编辑器中打开配置文件 |
| 关于 | 版本与构建号、官网与 GitHub 链接、许可证、随包数据来源、隐私说明、打开日志目录、打包日志到桌面、复制诊断信息 |

### 繁体输出

打开「繁体输出」后，候选与上屏文字按 OpenCC 转成繁体（只换字形，不换用语：软件 → 軟件）。云端词与整句补全同样转换。引擎内部仍按简体处理：用户词、词频、输入统计与输入日志都记简体，译词照常显示。

## 横排与拼音位置

横排时候选排成一行，仅高亮候选在下方单独一行显示译词，页码在行尾。
「只在窗口」显示拼音适用于终端与部分 Electron 应用，这类应用对行内拼音的渲染效果较差。

## 输入法菜单（macOS）

云联想开关、模糊音子菜单、偏好设置、打开日志目录、版本号。配置文件解析失败时增加一条灰色的错误说明，此时沿用上一份可用的配置，输入法不退出。

## 直接修改配置文件

全部设置保存在一个配置文件中，「高级」页有「在编辑器中打开配置文件」，文件内每一项均附说明。保存后一秒内生效，无需重新切换输入法。

五笔相关的项：`[general]` 分节的 `wubi` 留空为拼音，填 `86`、`98` 或 `xsj` 分别开启 86 五笔、98 五笔与新世纪五笔（新世纪也接受 `06` 与 `xinshiji`）；`[wubi]` 分节是五笔的行为：
`auto_select`（四码自动上屏，缺省开）、`hint`（编码提示，缺省开）、`fixed_order_length`（不超过这么多码的候选只按码表顺序、不随使用习惯调整，缺省 2）。随包码表只含常用字。

## 自定义短语（macOS）

「自定义短语」页以常驻表格显示启用状态、内容和固定候选位置，可直接勾选启用或停用。点击「+」新增，双击已有行或点击「编辑…」修改，选中后点击「−」删除。新增或编辑时，填写 1–32 个小写英文字母的输入码、1–9 的候选位置和文本，点击「保存规则」。文本可以包含空格、换行、地址、长回复或标点，按原样输出；候选窗口只展示长文本的简短预览。

例如将 `ee` 的第 1 位设置为 `：`，第 2 位设置为 `；`，输入 `ee` 后按空格选择首项，按 2 选择第二项。普通拼音候选填入其他位置，学习与模型重排不会改变自定义位置；云端词只使用不影响固定位置的剩余格。若固定位置超过第一页容量（例如第 9 位、每页 5 项），本次查询不显示云端词。位置跨页时按当前页的序号选择。

同一个输入码的位置不能重复，新增和编辑冲突时提示并拒绝保存；停用规则也保留其位置。没有足够普通候选时保留空位，空位不能上屏。选择已有规则后可编辑、停用或删除。

编辑期间若规则被其他窗口或外部文件修改，保存会提示并保留草稿，不覆盖新规则。保存会保留现有短语条目的注释。

默认中文标点模式会保存，重启后仍生效；选择半角不会改变自定义短语中的全角标点。英文模式不触发自定义短语。

### 系统的文本替换

「自定义短语」页勾选「也使用系统设置『键盘 → 文本替换』里的条目」（缺省勾选）后，系统文本替换中的条目在输入拼音时同样可用：
输入码敲全后短语出现在该输入码最靠前的空位（页里的规则优先占位），`Space` 或数字键上屏。
只有输入码全为小写英文字母的条目可用；系统设置中新增或修改的条目在下次切换到微明时生效。它们不在页里的表格中列出，要改请到系统设置。
